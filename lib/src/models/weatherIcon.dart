import 'package:flutter/material.dart';
import 'package:yummy_chat/src/models/palette.dart';

class WeatherIcon {
  static const Icon sunny = Icon(Icons.sunny, color: Palette.iconColor);
  static const Icon cloudy = Icon(Icons.cloud, color: Palette.iconColor);
  static const Icon rainy = Icon(Icons.umbrella, color: Palette.iconColor);
  static const Icon snowy = Icon(Icons.ac_unit, color: Palette.iconColor);
}
